# Tuned for a low-memory (4 GB) CI/sandbox: one JVM, no extra daemons.
org.gradle.jvmargs=-Xmx2600m -XX:MaxMetaspaceSize=640m -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.caching=true
org.gradle.configuration-cache=false

# Compile Kotlin + run KSP inside the Gradle JVM (no separate Kotlin daemon).
kotlin.compiler.execution.strategy=in-process
kotlin.incremental=false

# AndroidX
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin
kotlin.code.style=official
ksp.incremental=false
